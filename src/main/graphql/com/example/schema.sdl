type Query {
  viewer: Viewer!
}

type Viewer {
  books(input: BookInput!): [Book]!
}

type Book {
  id: String!
  name: String
  title: String
}

input BookInput {
  bookId: ID!
  version: Int @deprecated(reason: "Not needed anymore")
}