type Query {
  viewer: Viewer!
}

type Viewer {
  books: [Book]!
  something: [Unrelated]!
  justAField: String!
}

type Book {
  id: String!
  name: String
  title: String,
}

type Unrelated {
  id: String
  name: String
}
