type Query {
  viewer: Viewer!
}

type Viewer {
  books(input: BookInput!): [Book]!
}

type Book {
  id: String!
  name: String
  title: String
}

input BookInput {
  authorId: String
  deprecatedParameter: Int @deprecated(reason: "Not needed anymore")
}